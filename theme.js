(()=>{const key='wispcheckTheme';const root=document.documentElement;const saved=localStorage.getItem(key);const preferred=window.matchMedia&&window.matchMedia('(prefers-color-scheme: dark)').matches?'dark':'light';const apply=t=>{root.dataset.theme=t;document.querySelectorAll('.lightmode').forEach(el=>{el.textContent=t==='dark'?'☀ LIGHT':'◔ DARK';el.setAttribute('aria-label',t==='dark'?'Switch to light mode':'Switch to dark mode');el.setAttribute('role','button');el.setAttribute('tabindex','0');});};apply(saved||preferred);document.addEventListener('click',e=>{const el=e.target.closest('.lightmode');if(!el)return;const next=root.dataset.theme==='dark'?'light':'dark';localStorage.setItem(key,next);apply(next);});document.addEventListener('keydown',e=>{if((e.key==='Enter'||e.key===' ')&&e.target.classList?.contains('lightmode')){e.preventDefault();e.target.click();}});})();